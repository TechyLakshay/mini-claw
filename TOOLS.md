# TOOLS

Current tools:

- `tools/web_search.py`
  - searches the web and returns top results
- `tools/file_writer.py`
  - writes markdown files into `notes/`

Rules:
- only use tools that are implemented
- if a tool fails, report the error plainly
- do not claim browser, calendar, docs, or code sandbox tools unless they are added later
